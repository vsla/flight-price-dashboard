# URL da API do backend (sem barra no final)
# Desenvolvimento local:
NEXT_PUBLIC_API_URL=http://localhost:3001

# Produção (substitua pelo IP ou domínio da sua VM):
# NEXT_PUBLIC_API_URL=http://SEU_IP_DA_VM
